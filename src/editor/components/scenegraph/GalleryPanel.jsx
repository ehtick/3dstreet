import styles from './GalleryPanel.module.scss';

const GalleryPanel = () => {
  return (
    <div className={styles.galleryPanel}>
      <div className={styles.empty}>Gallery coming soon.</div>
    </div>
  );
};

export default GalleryPanel;
